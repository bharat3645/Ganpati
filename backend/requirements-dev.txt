# Extra dependencies for running the test suite locally / in CI.
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==8.3.3
httpx==0.27.2
